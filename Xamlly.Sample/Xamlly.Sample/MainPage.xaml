<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:xamlly="clr-namespace:Xamlly.XamllyControls;assembly=Xamlly"
             mc:Ignorable="d" BackgroundColor="Gray"
             x:Class="Xamlly.Sample.MainPage">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label" x:Key="titleStyle">
                <Setter Property="TextColor" Value="White"/>
                <Setter Property="HorizontalOptions" Value="CenterAndExpand"/>
                <Setter Property="FontAttributes" Value="Bold"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>
    <StackLayout x:Name="stkParent">
        <Label Text="Progress bar" Style="{StaticResource titleStyle}"/>
        <Button Text="Increment Progress" TextColor="Green" Clicked="Button_Clicked"/>
        <xamlly:ProgressBar Margin="0"
                            Progress=".3" ProgressColor="CadetBlue" OutlineColor="Black"
                            CornerRadius="10"
                            Padding="5"
                            ProgressTextColor="White"
                            BackgroundColor="Black"/>

        <xamlly:ProgressBar Margin="0"
                            Progress=".5" ProgressColor="DarkRed" OutlineColor="DarkRed"
                            CornerRadius="0"
                            Padding="4"
                            ProgressTextColor="Black"
                            BackgroundColor="White"/>

        <xamlly:ProgressBar Margin="0"
                            Progress=".7" ProgressColor="DarkOliveGreen" OutlineColor="Transparent"
                            CornerRadius="0"
                            Padding="8"
                            ProgressTextColor="Black"
                            BackgroundColor="DarkSeaGreen"/>
        <Label Text="Switches" Style="{StaticResource titleStyle}"/>

        <xamlly:Switch CornerRadius="0"  HorizontalOptions="Center"
                       ButtonWidth="70" FontSize="15"
                       OffText="Disagree" OnText="Agree"/>

        <xamlly:Switch CornerRadius="10" ButtonWidth="30" HorizontalOptions="Center"
                       OnColor="Red"
                       OffText="No" OnText="Yes"/>

        <Label Text="Toggle button" Style="{StaticResource titleStyle}"/>
        <xamlly:ToggleButton Text="ON" SelectedColor="White" UnselectedColor="Gray" BackgroundColor="Black" HorizontalOptions="CenterAndExpand"/>
        <Label Text="Toggles bar" Style="{StaticResource titleStyle}"/>

        <xamlly:TogglesBar ItemsSource="{Binding Colors}" DisplayMemberPath="Name" SelectedColor="White" UnselectedColor="Gray" BackgroundColor="Black" HorizontalOptions="Fill"/>
        <xamlly:TogglesBar IsMultiSelect="True" ItemsSource="{Binding Colors}" DisplayMemberPath="Name" SelectedColor="White" UnselectedColor="Gray" BackgroundColor="Black" HorizontalOptions="Fill"/>

        <Label Text="RadioButtonsGroup" Style="{StaticResource titleStyle}"/>
        <xamlly:RadioButtonsGroup HorizontalOptions="CenterAndExpand" ItemsSource="{Binding Colors}" DisplayMemberPath="Name" SelectedValuePath="ID" 
                                  SelectedIndex="2" OnSelectionChanged="rbg_OnSelectionChanged" RadioButtonColor="White" 
                                  TextColor="White"/>

        <xamlly:RadioButtonsGroup HorizontalOptions="FillAndExpand" ItemsSource="{Binding Colors}" Orientation="Horizontal" DisplayMemberPath="Name" SelectedValuePath="ID" 
                                  SelectedIndex="2" OnSelectionChanged="rbg_OnSelectionChanged" 
                                  RadioButtonColor="Khaki" 
                                  TextColor="Orange"/>
    </StackLayout>



</ContentPage>
